<% @title = @usuario.nome %>

<h2><%= @usuario.nome %></h2>

<ul>
	<% @image.each do |image| %>
		<li><%= link_to (image_tag image.photo.url(:small)), :controller => "images", :action => "show", :id => image.id %></li>	
	<% end %>
</ul>
